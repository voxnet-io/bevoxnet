node_modules

#Hardhat files
cache
artifacts

# Secrets: ignore every .env* variant, but keep the committed templates.
.env*
!.env.example
!.env.prod.example

# Internal-only working docs (kept locally, not published)
internal/

# Project continuity tracker (local working state, not distributable source)
progress-notes/

# Local Hardhat chain deployment artifact (meaningless off-chain)
deployments-1337.json

# AI / engineering-standards working doc — kept local, not published
/CLAUDE.MD

# VS Code workspace file — local editor config, not published
/bevoxnet.code-workspace
